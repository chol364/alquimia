declare global {
    var Go: new () => {
        argv: string[];
        env: Record<string, string>;
        importObject: WebAssembly.Imports;
        run: (inst: WebAssembly.Instance) => Promise<void>;
    };
}
export type InitOptions = {
    argv?: string[];
    env?: Record<string, string>;
    namespace?: string;
    imports?: WebAssembly.Imports;
    wasmUrl?: string;
};
export declare function initGoWasm<TApi = Record<string, unknown>>(opts?: InitOptions): Promise<{
    instance: WebAssembly.Instance;
    api: TApi | undefined;
    done: Promise<void>;
}>;
