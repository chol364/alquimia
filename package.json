{
  "name": "projeto-labelary2.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install-all": "npm install --workspaces",
    "build": "npm run build --workspaces",
    "start": "npm --workspace backend start"
  }
}